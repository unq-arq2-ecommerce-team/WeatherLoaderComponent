basePath: /
definitions:
  infrastructure.HealthCheckRes:
    properties:
      data:
        type: string
    type: object
info:
  contact:
    email: support@swagger.io
    name: API SUPPORT
    url: http://www.swagger.io/support
  description: api for final tp arq2
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  title: Weather Loader Component API
  version: "1.0"
paths:
  /:
    get:
      consumes:
      - '*/*'
      description: get the status of server.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/infrastructure.HealthCheckRes'
      summary: Show the status of server.
      tags:
      - Health check
swagger: "2.0"
